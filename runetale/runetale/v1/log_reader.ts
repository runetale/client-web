// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.20.3
// source: runetale/runetale/v1/log_reader.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "logserver";

/** GetLoglyphRequest queries client debug log entries. */
export interface GetLoglyphRequest {
  /** log_stream_id is the PublicID (SHA-256 of PrivateID) to query. */
  logStreamId: string;
  /** from is the start of the time range (inclusive). */
  from:
    | Date
    | undefined;
  /** to is the end of the time range (exclusive). */
  to:
    | Date
    | undefined;
  /** limit is the maximum number of entries to return. */
  limit: number;
  /** offset is the number of entries to skip (for pagination). */
  offset: number;
  /** session_id is an optional filter to restrict results to a single session. */
  sessionId: string;
  /** level is an optional filter for log level (e.g., "error", "warn"). */
  level: string;
  /**
   * telemetry_log_id is the server-provided node-level telemetry ID for admin queries.
   * If set, queries entries by this ID instead of log_stream_id.
   */
  telemetryLogId: string;
}

/** GetLoglyphResponse contains the queried log entries. */
export interface GetLoglyphResponse {
  /** entries is the list of log entries matching the query. */
  entries: StoredLoglyphEntry[];
  /** total_count is the total number of matching entries (for pagination). */
  totalCount: number;
}

/** StoredLoglyphEntry is a log entry as stored in the database. */
export interface StoredLoglyphEntry {
  id: number;
  logStreamId: string;
  sessionId: string;
  clientVersion: string;
  clientTime: Date | undefined;
  receivedAt: Date | undefined;
  level: string;
  text: string;
  payload: string;
  createdAt:
    | Date
    | undefined;
  /** telemetry_log_id is the server-provided node-level ID for admin correlation. */
  telemetryLogId: string;
}

/** GetOrbitEventsRequest queries stored telemetry events. */
export interface GetOrbitEventsRequest {
  /** log_stream_id is the PublicID to query. */
  logStreamId: string;
  /** session_id is an optional filter for a specific session. */
  sessionId: string;
  /** from is the start of the time range (inclusive). */
  from:
    | Date
    | undefined;
  /** to is the end of the time range (exclusive). */
  to:
    | Date
    | undefined;
  /** limit is the maximum number of events to return. */
  limit: number;
  /** offset is the number of events to skip (for pagination). */
  offset: number;
  /** payload_type is an optional filter (e.g., "send_result", "path_transition"). */
  payloadType: string;
  /**
   * telemetry_log_id is the server-provided node-level telemetry ID for admin queries.
   * If set, queries events by this ID instead of log_stream_id.
   */
  telemetryLogId: string;
}

/** GetOrbitEventsResponse contains the queried events. */
export interface GetOrbitEventsResponse {
  /** events is the list of events matching the query. */
  events: StoredOrbitEvent[];
  /** total_count is the total number of matching events (for pagination). */
  totalCount: number;
}

/** StoredOrbitEvent is a telemetry event as stored in the database. */
export interface StoredOrbitEvent {
  id: number;
  logStreamId: string;
  sessionId: string;
  clientVersion: string;
  receivedAt: Date | undefined;
  eventAt: Date | undefined;
  eventDay: Date | undefined;
  peerHash: Uint8Array;
  regionId: number;
  transport: number;
  payloadType: string;
  /** JSON-encoded payload */
  payload: string;
  createdAt:
    | Date
    | undefined;
  /** telemetry_log_id is the server-provided node-level ID for admin correlation. */
  telemetryLogId: string;
}

/** GetOrbitDailyCountsRequest queries aggregated daily telemetry counts. */
export interface GetOrbitDailyCountsRequest {
  /** log_stream_id is the PublicID to query. */
  logStreamId: string;
  /** metric is an optional filter for a specific metric name. */
  metric: string;
  /** from is the start of the time range (inclusive). */
  from:
    | Date
    | undefined;
  /** to is the end of the time range (exclusive). */
  to: Date | undefined;
}

/** GetOrbitDailyCountsResponse contains the aggregated counts. */
export interface GetOrbitDailyCountsResponse {
  /** counts is the list of daily counts matching the query. */
  counts: DailyCount[];
}

/** DailyCount represents an aggregated count for a day. */
export interface DailyCount {
  day: Date | undefined;
  logStreamId: string;
  metric: string;
  labelKey: string;
  count: number;
  createdAt: Date | undefined;
  updatedAt: Date | undefined;
}

/** GetPacketFlowLogsRequest queries network flow statistics. */
export interface GetPacketFlowLogsRequest {
  /** log_stream_id is the PublicID to query. */
  logStreamId: string;
  /** from is the start of the time range (inclusive). */
  from:
    | Date
    | undefined;
  /** to is the end of the time range (exclusive). */
  to:
    | Date
    | undefined;
  /** limit is the maximum number of records to return. */
  limit: number;
  /** offset is the number of records to skip (for pagination). */
  offset: number;
  /** node_type is an optional filter (e.g., "peer", "lan", "exit", "transport"). */
  nodeType: string;
  /**
   * domain_telemetry_log_id is the server-provided tenant-level telemetry ID for admin queries.
   * If set, queries logs by this ID instead of log_stream_id.
   */
  domainTelemetryLogId: string;
  /**
   * telemetry_log_id is the server-provided node-level telemetry ID for per-node queries.
   * Priority: domain_telemetry_log_id > telemetry_log_id > log_stream_id.
   */
  telemetryLogId: string;
}

/** GetPacketFlowLogsResponse contains the queried flow logs. */
export interface GetPacketFlowLogsResponse {
  /** logs is the list of flow records matching the query. */
  logs: StoredPacketFlowLog[];
  /** total_count is the total number of matching records (for pagination). */
  totalCount: number;
}

/** StoredPacketFlowLog is a flow record as stored in the database. */
export interface StoredPacketFlowLog {
  id: number;
  logStreamId: string;
  /** peer/lan/exit/transport */
  nodeType: string;
  /** tcp/udp/icmpv4/icmpv6 */
  protocol: string;
  srcIp: string;
  dstIp: string;
  txPackets: number;
  txBytes: number;
  rxPackets: number;
  rxBytes: number;
  startedAt: Date | undefined;
  endedAt: Date | undefined;
  loggedAt: Date | undefined;
  createdAt:
    | Date
    | undefined;
  /** domain_telemetry_log_id is the server-provided tenant-level ID for admin correlation. */
  domainTelemetryLogId: string;
  /** Source node identity (A-plan: embedded in payload for self-contained SIEM export). */
  telemetryLogId: string;
  /** source hostname (e.g. "alice-macbook.runep9anv.runet.") */
  nodeName: string;
  /** source user email (e.g. "alice@example.com") */
  userEmail: string;
  /**
   * Destination node identity (resolved from dst_peers, Tailscale-style deduplication).
   * Only populated for peer traffic (Runetale IP <-> Runetale IP).
   */
  dstNodeName: string;
  /** destination user email */
  dstUserEmail: string;
  /** destination node ID */
  dstNodeId: string;
}

function createBaseGetLoglyphRequest(): GetLoglyphRequest {
  return {
    logStreamId: "",
    from: undefined,
    to: undefined,
    limit: 0,
    offset: 0,
    sessionId: "",
    level: "",
    telemetryLogId: "",
  };
}

export const GetLoglyphRequest: MessageFns<GetLoglyphRequest> = {
  encode(message: GetLoglyphRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.logStreamId !== "") {
      writer.uint32(10).string(message.logStreamId);
    }
    if (message.from !== undefined) {
      Timestamp.encode(toTimestamp(message.from), writer.uint32(18).fork()).join();
    }
    if (message.to !== undefined) {
      Timestamp.encode(toTimestamp(message.to), writer.uint32(26).fork()).join();
    }
    if (message.limit !== 0) {
      writer.uint32(32).int32(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(40).int32(message.offset);
    }
    if (message.sessionId !== "") {
      writer.uint32(50).string(message.sessionId);
    }
    if (message.level !== "") {
      writer.uint32(58).string(message.level);
    }
    if (message.telemetryLogId !== "") {
      writer.uint32(66).string(message.telemetryLogId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLoglyphRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLoglyphRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logStreamId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.from = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.to = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.offset = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.level = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.telemetryLogId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLoglyphRequest {
    return {
      logStreamId: isSet(object.logStreamId)
        ? globalThis.String(object.logStreamId)
        : isSet(object.log_stream_id)
        ? globalThis.String(object.log_stream_id)
        : "",
      from: isSet(object.from) ? fromJsonTimestamp(object.from) : undefined,
      to: isSet(object.to) ? fromJsonTimestamp(object.to) : undefined,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      level: isSet(object.level) ? globalThis.String(object.level) : "",
      telemetryLogId: isSet(object.telemetryLogId)
        ? globalThis.String(object.telemetryLogId)
        : isSet(object.telemetry_log_id)
        ? globalThis.String(object.telemetry_log_id)
        : "",
    };
  },

  toJSON(message: GetLoglyphRequest): unknown {
    const obj: any = {};
    if (message.logStreamId !== "") {
      obj.logStreamId = message.logStreamId;
    }
    if (message.from !== undefined) {
      obj.from = message.from.toISOString();
    }
    if (message.to !== undefined) {
      obj.to = message.to.toISOString();
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.level !== "") {
      obj.level = message.level;
    }
    if (message.telemetryLogId !== "") {
      obj.telemetryLogId = message.telemetryLogId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetLoglyphRequest>, I>>(base?: I): GetLoglyphRequest {
    return GetLoglyphRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLoglyphRequest>, I>>(object: I): GetLoglyphRequest {
    const message = createBaseGetLoglyphRequest();
    message.logStreamId = object.logStreamId ?? "";
    message.from = object.from ?? undefined;
    message.to = object.to ?? undefined;
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    message.sessionId = object.sessionId ?? "";
    message.level = object.level ?? "";
    message.telemetryLogId = object.telemetryLogId ?? "";
    return message;
  },
};

function createBaseGetLoglyphResponse(): GetLoglyphResponse {
  return { entries: [], totalCount: 0 };
}

export const GetLoglyphResponse: MessageFns<GetLoglyphResponse> = {
  encode(message: GetLoglyphResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      StoredLoglyphEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalCount !== 0) {
      writer.uint32(16).int64(message.totalCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLoglyphResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLoglyphResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(StoredLoglyphEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalCount = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLoglyphResponse {
    return {
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => StoredLoglyphEntry.fromJSON(e))
        : [],
      totalCount: isSet(object.totalCount)
        ? globalThis.Number(object.totalCount)
        : isSet(object.total_count)
        ? globalThis.Number(object.total_count)
        : 0,
    };
  },

  toJSON(message: GetLoglyphResponse): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => StoredLoglyphEntry.toJSON(e));
    }
    if (message.totalCount !== 0) {
      obj.totalCount = Math.round(message.totalCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetLoglyphResponse>, I>>(base?: I): GetLoglyphResponse {
    return GetLoglyphResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLoglyphResponse>, I>>(object: I): GetLoglyphResponse {
    const message = createBaseGetLoglyphResponse();
    message.entries = object.entries?.map((e) => StoredLoglyphEntry.fromPartial(e)) || [];
    message.totalCount = object.totalCount ?? 0;
    return message;
  },
};

function createBaseStoredLoglyphEntry(): StoredLoglyphEntry {
  return {
    id: 0,
    logStreamId: "",
    sessionId: "",
    clientVersion: "",
    clientTime: undefined,
    receivedAt: undefined,
    level: "",
    text: "",
    payload: "",
    createdAt: undefined,
    telemetryLogId: "",
  };
}

export const StoredLoglyphEntry: MessageFns<StoredLoglyphEntry> = {
  encode(message: StoredLoglyphEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.logStreamId !== "") {
      writer.uint32(18).string(message.logStreamId);
    }
    if (message.sessionId !== "") {
      writer.uint32(26).string(message.sessionId);
    }
    if (message.clientVersion !== "") {
      writer.uint32(34).string(message.clientVersion);
    }
    if (message.clientTime !== undefined) {
      Timestamp.encode(toTimestamp(message.clientTime), writer.uint32(42).fork()).join();
    }
    if (message.receivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.receivedAt), writer.uint32(50).fork()).join();
    }
    if (message.level !== "") {
      writer.uint32(58).string(message.level);
    }
    if (message.text !== "") {
      writer.uint32(66).string(message.text);
    }
    if (message.payload !== "") {
      writer.uint32(74).string(message.payload);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(82).fork()).join();
    }
    if (message.telemetryLogId !== "") {
      writer.uint32(90).string(message.telemetryLogId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredLoglyphEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredLoglyphEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.logStreamId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.clientVersion = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.clientTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.receivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.level = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.payload = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.telemetryLogId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredLoglyphEntry {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      logStreamId: isSet(object.logStreamId)
        ? globalThis.String(object.logStreamId)
        : isSet(object.log_stream_id)
        ? globalThis.String(object.log_stream_id)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      clientVersion: isSet(object.clientVersion)
        ? globalThis.String(object.clientVersion)
        : isSet(object.client_version)
        ? globalThis.String(object.client_version)
        : "",
      clientTime: isSet(object.clientTime)
        ? fromJsonTimestamp(object.clientTime)
        : isSet(object.client_time)
        ? fromJsonTimestamp(object.client_time)
        : undefined,
      receivedAt: isSet(object.receivedAt)
        ? fromJsonTimestamp(object.receivedAt)
        : isSet(object.received_at)
        ? fromJsonTimestamp(object.received_at)
        : undefined,
      level: isSet(object.level) ? globalThis.String(object.level) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      payload: isSet(object.payload) ? globalThis.String(object.payload) : "",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      telemetryLogId: isSet(object.telemetryLogId)
        ? globalThis.String(object.telemetryLogId)
        : isSet(object.telemetry_log_id)
        ? globalThis.String(object.telemetry_log_id)
        : "",
    };
  },

  toJSON(message: StoredLoglyphEntry): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.logStreamId !== "") {
      obj.logStreamId = message.logStreamId;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.clientVersion !== "") {
      obj.clientVersion = message.clientVersion;
    }
    if (message.clientTime !== undefined) {
      obj.clientTime = message.clientTime.toISOString();
    }
    if (message.receivedAt !== undefined) {
      obj.receivedAt = message.receivedAt.toISOString();
    }
    if (message.level !== "") {
      obj.level = message.level;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.payload !== "") {
      obj.payload = message.payload;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.telemetryLogId !== "") {
      obj.telemetryLogId = message.telemetryLogId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredLoglyphEntry>, I>>(base?: I): StoredLoglyphEntry {
    return StoredLoglyphEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredLoglyphEntry>, I>>(object: I): StoredLoglyphEntry {
    const message = createBaseStoredLoglyphEntry();
    message.id = object.id ?? 0;
    message.logStreamId = object.logStreamId ?? "";
    message.sessionId = object.sessionId ?? "";
    message.clientVersion = object.clientVersion ?? "";
    message.clientTime = object.clientTime ?? undefined;
    message.receivedAt = object.receivedAt ?? undefined;
    message.level = object.level ?? "";
    message.text = object.text ?? "";
    message.payload = object.payload ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.telemetryLogId = object.telemetryLogId ?? "";
    return message;
  },
};

function createBaseGetOrbitEventsRequest(): GetOrbitEventsRequest {
  return {
    logStreamId: "",
    sessionId: "",
    from: undefined,
    to: undefined,
    limit: 0,
    offset: 0,
    payloadType: "",
    telemetryLogId: "",
  };
}

export const GetOrbitEventsRequest: MessageFns<GetOrbitEventsRequest> = {
  encode(message: GetOrbitEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.logStreamId !== "") {
      writer.uint32(10).string(message.logStreamId);
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.from !== undefined) {
      Timestamp.encode(toTimestamp(message.from), writer.uint32(26).fork()).join();
    }
    if (message.to !== undefined) {
      Timestamp.encode(toTimestamp(message.to), writer.uint32(34).fork()).join();
    }
    if (message.limit !== 0) {
      writer.uint32(40).int32(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(48).int32(message.offset);
    }
    if (message.payloadType !== "") {
      writer.uint32(58).string(message.payloadType);
    }
    if (message.telemetryLogId !== "") {
      writer.uint32(66).string(message.telemetryLogId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetOrbitEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOrbitEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logStreamId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.from = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.to = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.offset = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.payloadType = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.telemetryLogId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOrbitEventsRequest {
    return {
      logStreamId: isSet(object.logStreamId)
        ? globalThis.String(object.logStreamId)
        : isSet(object.log_stream_id)
        ? globalThis.String(object.log_stream_id)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      from: isSet(object.from) ? fromJsonTimestamp(object.from) : undefined,
      to: isSet(object.to) ? fromJsonTimestamp(object.to) : undefined,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
      payloadType: isSet(object.payloadType)
        ? globalThis.String(object.payloadType)
        : isSet(object.payload_type)
        ? globalThis.String(object.payload_type)
        : "",
      telemetryLogId: isSet(object.telemetryLogId)
        ? globalThis.String(object.telemetryLogId)
        : isSet(object.telemetry_log_id)
        ? globalThis.String(object.telemetry_log_id)
        : "",
    };
  },

  toJSON(message: GetOrbitEventsRequest): unknown {
    const obj: any = {};
    if (message.logStreamId !== "") {
      obj.logStreamId = message.logStreamId;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.from !== undefined) {
      obj.from = message.from.toISOString();
    }
    if (message.to !== undefined) {
      obj.to = message.to.toISOString();
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.payloadType !== "") {
      obj.payloadType = message.payloadType;
    }
    if (message.telemetryLogId !== "") {
      obj.telemetryLogId = message.telemetryLogId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetOrbitEventsRequest>, I>>(base?: I): GetOrbitEventsRequest {
    return GetOrbitEventsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetOrbitEventsRequest>, I>>(object: I): GetOrbitEventsRequest {
    const message = createBaseGetOrbitEventsRequest();
    message.logStreamId = object.logStreamId ?? "";
    message.sessionId = object.sessionId ?? "";
    message.from = object.from ?? undefined;
    message.to = object.to ?? undefined;
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    message.payloadType = object.payloadType ?? "";
    message.telemetryLogId = object.telemetryLogId ?? "";
    return message;
  },
};

function createBaseGetOrbitEventsResponse(): GetOrbitEventsResponse {
  return { events: [], totalCount: 0 };
}

export const GetOrbitEventsResponse: MessageFns<GetOrbitEventsResponse> = {
  encode(message: GetOrbitEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.events) {
      StoredOrbitEvent.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalCount !== 0) {
      writer.uint32(16).int64(message.totalCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetOrbitEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOrbitEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.events.push(StoredOrbitEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalCount = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOrbitEventsResponse {
    return {
      events: globalThis.Array.isArray(object?.events)
        ? object.events.map((e: any) => StoredOrbitEvent.fromJSON(e))
        : [],
      totalCount: isSet(object.totalCount)
        ? globalThis.Number(object.totalCount)
        : isSet(object.total_count)
        ? globalThis.Number(object.total_count)
        : 0,
    };
  },

  toJSON(message: GetOrbitEventsResponse): unknown {
    const obj: any = {};
    if (message.events?.length) {
      obj.events = message.events.map((e) => StoredOrbitEvent.toJSON(e));
    }
    if (message.totalCount !== 0) {
      obj.totalCount = Math.round(message.totalCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetOrbitEventsResponse>, I>>(base?: I): GetOrbitEventsResponse {
    return GetOrbitEventsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetOrbitEventsResponse>, I>>(object: I): GetOrbitEventsResponse {
    const message = createBaseGetOrbitEventsResponse();
    message.events = object.events?.map((e) => StoredOrbitEvent.fromPartial(e)) || [];
    message.totalCount = object.totalCount ?? 0;
    return message;
  },
};

function createBaseStoredOrbitEvent(): StoredOrbitEvent {
  return {
    id: 0,
    logStreamId: "",
    sessionId: "",
    clientVersion: "",
    receivedAt: undefined,
    eventAt: undefined,
    eventDay: undefined,
    peerHash: new Uint8Array(0),
    regionId: 0,
    transport: 0,
    payloadType: "",
    payload: "",
    createdAt: undefined,
    telemetryLogId: "",
  };
}

export const StoredOrbitEvent: MessageFns<StoredOrbitEvent> = {
  encode(message: StoredOrbitEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.logStreamId !== "") {
      writer.uint32(18).string(message.logStreamId);
    }
    if (message.sessionId !== "") {
      writer.uint32(26).string(message.sessionId);
    }
    if (message.clientVersion !== "") {
      writer.uint32(34).string(message.clientVersion);
    }
    if (message.receivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.receivedAt), writer.uint32(42).fork()).join();
    }
    if (message.eventAt !== undefined) {
      Timestamp.encode(toTimestamp(message.eventAt), writer.uint32(50).fork()).join();
    }
    if (message.eventDay !== undefined) {
      Timestamp.encode(toTimestamp(message.eventDay), writer.uint32(58).fork()).join();
    }
    if (message.peerHash.length !== 0) {
      writer.uint32(66).bytes(message.peerHash);
    }
    if (message.regionId !== 0) {
      writer.uint32(72).uint32(message.regionId);
    }
    if (message.transport !== 0) {
      writer.uint32(80).int32(message.transport);
    }
    if (message.payloadType !== "") {
      writer.uint32(90).string(message.payloadType);
    }
    if (message.payload !== "") {
      writer.uint32(98).string(message.payload);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(106).fork()).join();
    }
    if (message.telemetryLogId !== "") {
      writer.uint32(114).string(message.telemetryLogId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredOrbitEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredOrbitEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.logStreamId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.clientVersion = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.receivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.eventAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.eventDay = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.peerHash = reader.bytes();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.regionId = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.transport = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.payloadType = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.payload = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.telemetryLogId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredOrbitEvent {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      logStreamId: isSet(object.logStreamId)
        ? globalThis.String(object.logStreamId)
        : isSet(object.log_stream_id)
        ? globalThis.String(object.log_stream_id)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      clientVersion: isSet(object.clientVersion)
        ? globalThis.String(object.clientVersion)
        : isSet(object.client_version)
        ? globalThis.String(object.client_version)
        : "",
      receivedAt: isSet(object.receivedAt)
        ? fromJsonTimestamp(object.receivedAt)
        : isSet(object.received_at)
        ? fromJsonTimestamp(object.received_at)
        : undefined,
      eventAt: isSet(object.eventAt)
        ? fromJsonTimestamp(object.eventAt)
        : isSet(object.event_at)
        ? fromJsonTimestamp(object.event_at)
        : undefined,
      eventDay: isSet(object.eventDay)
        ? fromJsonTimestamp(object.eventDay)
        : isSet(object.event_day)
        ? fromJsonTimestamp(object.event_day)
        : undefined,
      peerHash: isSet(object.peerHash)
        ? bytesFromBase64(object.peerHash)
        : isSet(object.peer_hash)
        ? bytesFromBase64(object.peer_hash)
        : new Uint8Array(0),
      regionId: isSet(object.regionId)
        ? globalThis.Number(object.regionId)
        : isSet(object.region_id)
        ? globalThis.Number(object.region_id)
        : 0,
      transport: isSet(object.transport) ? globalThis.Number(object.transport) : 0,
      payloadType: isSet(object.payloadType)
        ? globalThis.String(object.payloadType)
        : isSet(object.payload_type)
        ? globalThis.String(object.payload_type)
        : "",
      payload: isSet(object.payload) ? globalThis.String(object.payload) : "",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      telemetryLogId: isSet(object.telemetryLogId)
        ? globalThis.String(object.telemetryLogId)
        : isSet(object.telemetry_log_id)
        ? globalThis.String(object.telemetry_log_id)
        : "",
    };
  },

  toJSON(message: StoredOrbitEvent): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.logStreamId !== "") {
      obj.logStreamId = message.logStreamId;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.clientVersion !== "") {
      obj.clientVersion = message.clientVersion;
    }
    if (message.receivedAt !== undefined) {
      obj.receivedAt = message.receivedAt.toISOString();
    }
    if (message.eventAt !== undefined) {
      obj.eventAt = message.eventAt.toISOString();
    }
    if (message.eventDay !== undefined) {
      obj.eventDay = message.eventDay.toISOString();
    }
    if (message.peerHash.length !== 0) {
      obj.peerHash = base64FromBytes(message.peerHash);
    }
    if (message.regionId !== 0) {
      obj.regionId = Math.round(message.regionId);
    }
    if (message.transport !== 0) {
      obj.transport = Math.round(message.transport);
    }
    if (message.payloadType !== "") {
      obj.payloadType = message.payloadType;
    }
    if (message.payload !== "") {
      obj.payload = message.payload;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.telemetryLogId !== "") {
      obj.telemetryLogId = message.telemetryLogId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredOrbitEvent>, I>>(base?: I): StoredOrbitEvent {
    return StoredOrbitEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredOrbitEvent>, I>>(object: I): StoredOrbitEvent {
    const message = createBaseStoredOrbitEvent();
    message.id = object.id ?? 0;
    message.logStreamId = object.logStreamId ?? "";
    message.sessionId = object.sessionId ?? "";
    message.clientVersion = object.clientVersion ?? "";
    message.receivedAt = object.receivedAt ?? undefined;
    message.eventAt = object.eventAt ?? undefined;
    message.eventDay = object.eventDay ?? undefined;
    message.peerHash = object.peerHash ?? new Uint8Array(0);
    message.regionId = object.regionId ?? 0;
    message.transport = object.transport ?? 0;
    message.payloadType = object.payloadType ?? "";
    message.payload = object.payload ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.telemetryLogId = object.telemetryLogId ?? "";
    return message;
  },
};

function createBaseGetOrbitDailyCountsRequest(): GetOrbitDailyCountsRequest {
  return { logStreamId: "", metric: "", from: undefined, to: undefined };
}

export const GetOrbitDailyCountsRequest: MessageFns<GetOrbitDailyCountsRequest> = {
  encode(message: GetOrbitDailyCountsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.logStreamId !== "") {
      writer.uint32(10).string(message.logStreamId);
    }
    if (message.metric !== "") {
      writer.uint32(18).string(message.metric);
    }
    if (message.from !== undefined) {
      Timestamp.encode(toTimestamp(message.from), writer.uint32(26).fork()).join();
    }
    if (message.to !== undefined) {
      Timestamp.encode(toTimestamp(message.to), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetOrbitDailyCountsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOrbitDailyCountsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logStreamId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.metric = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.from = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.to = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOrbitDailyCountsRequest {
    return {
      logStreamId: isSet(object.logStreamId)
        ? globalThis.String(object.logStreamId)
        : isSet(object.log_stream_id)
        ? globalThis.String(object.log_stream_id)
        : "",
      metric: isSet(object.metric) ? globalThis.String(object.metric) : "",
      from: isSet(object.from) ? fromJsonTimestamp(object.from) : undefined,
      to: isSet(object.to) ? fromJsonTimestamp(object.to) : undefined,
    };
  },

  toJSON(message: GetOrbitDailyCountsRequest): unknown {
    const obj: any = {};
    if (message.logStreamId !== "") {
      obj.logStreamId = message.logStreamId;
    }
    if (message.metric !== "") {
      obj.metric = message.metric;
    }
    if (message.from !== undefined) {
      obj.from = message.from.toISOString();
    }
    if (message.to !== undefined) {
      obj.to = message.to.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetOrbitDailyCountsRequest>, I>>(base?: I): GetOrbitDailyCountsRequest {
    return GetOrbitDailyCountsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetOrbitDailyCountsRequest>, I>>(object: I): GetOrbitDailyCountsRequest {
    const message = createBaseGetOrbitDailyCountsRequest();
    message.logStreamId = object.logStreamId ?? "";
    message.metric = object.metric ?? "";
    message.from = object.from ?? undefined;
    message.to = object.to ?? undefined;
    return message;
  },
};

function createBaseGetOrbitDailyCountsResponse(): GetOrbitDailyCountsResponse {
  return { counts: [] };
}

export const GetOrbitDailyCountsResponse: MessageFns<GetOrbitDailyCountsResponse> = {
  encode(message: GetOrbitDailyCountsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.counts) {
      DailyCount.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetOrbitDailyCountsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOrbitDailyCountsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.counts.push(DailyCount.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOrbitDailyCountsResponse {
    return {
      counts: globalThis.Array.isArray(object?.counts) ? object.counts.map((e: any) => DailyCount.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetOrbitDailyCountsResponse): unknown {
    const obj: any = {};
    if (message.counts?.length) {
      obj.counts = message.counts.map((e) => DailyCount.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetOrbitDailyCountsResponse>, I>>(base?: I): GetOrbitDailyCountsResponse {
    return GetOrbitDailyCountsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetOrbitDailyCountsResponse>, I>>(object: I): GetOrbitDailyCountsResponse {
    const message = createBaseGetOrbitDailyCountsResponse();
    message.counts = object.counts?.map((e) => DailyCount.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDailyCount(): DailyCount {
  return {
    day: undefined,
    logStreamId: "",
    metric: "",
    labelKey: "",
    count: 0,
    createdAt: undefined,
    updatedAt: undefined,
  };
}

export const DailyCount: MessageFns<DailyCount> = {
  encode(message: DailyCount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.day !== undefined) {
      Timestamp.encode(toTimestamp(message.day), writer.uint32(10).fork()).join();
    }
    if (message.logStreamId !== "") {
      writer.uint32(18).string(message.logStreamId);
    }
    if (message.metric !== "") {
      writer.uint32(26).string(message.metric);
    }
    if (message.labelKey !== "") {
      writer.uint32(34).string(message.labelKey);
    }
    if (message.count !== 0) {
      writer.uint32(40).int64(message.count);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(50).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DailyCount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDailyCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.day = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.logStreamId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.metric = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.labelKey = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.count = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DailyCount {
    return {
      day: isSet(object.day) ? fromJsonTimestamp(object.day) : undefined,
      logStreamId: isSet(object.logStreamId)
        ? globalThis.String(object.logStreamId)
        : isSet(object.log_stream_id)
        ? globalThis.String(object.log_stream_id)
        : "",
      metric: isSet(object.metric) ? globalThis.String(object.metric) : "",
      labelKey: isSet(object.labelKey)
        ? globalThis.String(object.labelKey)
        : isSet(object.label_key)
        ? globalThis.String(object.label_key)
        : "",
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: DailyCount): unknown {
    const obj: any = {};
    if (message.day !== undefined) {
      obj.day = message.day.toISOString();
    }
    if (message.logStreamId !== "") {
      obj.logStreamId = message.logStreamId;
    }
    if (message.metric !== "") {
      obj.metric = message.metric;
    }
    if (message.labelKey !== "") {
      obj.labelKey = message.labelKey;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DailyCount>, I>>(base?: I): DailyCount {
    return DailyCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DailyCount>, I>>(object: I): DailyCount {
    const message = createBaseDailyCount();
    message.day = object.day ?? undefined;
    message.logStreamId = object.logStreamId ?? "";
    message.metric = object.metric ?? "";
    message.labelKey = object.labelKey ?? "";
    message.count = object.count ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseGetPacketFlowLogsRequest(): GetPacketFlowLogsRequest {
  return {
    logStreamId: "",
    from: undefined,
    to: undefined,
    limit: 0,
    offset: 0,
    nodeType: "",
    domainTelemetryLogId: "",
    telemetryLogId: "",
  };
}

export const GetPacketFlowLogsRequest: MessageFns<GetPacketFlowLogsRequest> = {
  encode(message: GetPacketFlowLogsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.logStreamId !== "") {
      writer.uint32(10).string(message.logStreamId);
    }
    if (message.from !== undefined) {
      Timestamp.encode(toTimestamp(message.from), writer.uint32(18).fork()).join();
    }
    if (message.to !== undefined) {
      Timestamp.encode(toTimestamp(message.to), writer.uint32(26).fork()).join();
    }
    if (message.limit !== 0) {
      writer.uint32(32).int32(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(40).int32(message.offset);
    }
    if (message.nodeType !== "") {
      writer.uint32(50).string(message.nodeType);
    }
    if (message.domainTelemetryLogId !== "") {
      writer.uint32(58).string(message.domainTelemetryLogId);
    }
    if (message.telemetryLogId !== "") {
      writer.uint32(66).string(message.telemetryLogId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPacketFlowLogsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPacketFlowLogsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logStreamId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.from = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.to = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.offset = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.nodeType = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.domainTelemetryLogId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.telemetryLogId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPacketFlowLogsRequest {
    return {
      logStreamId: isSet(object.logStreamId)
        ? globalThis.String(object.logStreamId)
        : isSet(object.log_stream_id)
        ? globalThis.String(object.log_stream_id)
        : "",
      from: isSet(object.from) ? fromJsonTimestamp(object.from) : undefined,
      to: isSet(object.to) ? fromJsonTimestamp(object.to) : undefined,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
      nodeType: isSet(object.nodeType)
        ? globalThis.String(object.nodeType)
        : isSet(object.node_type)
        ? globalThis.String(object.node_type)
        : "",
      domainTelemetryLogId: isSet(object.domainTelemetryLogId)
        ? globalThis.String(object.domainTelemetryLogId)
        : isSet(object.domain_telemetry_log_id)
        ? globalThis.String(object.domain_telemetry_log_id)
        : "",
      telemetryLogId: isSet(object.telemetryLogId)
        ? globalThis.String(object.telemetryLogId)
        : isSet(object.telemetry_log_id)
        ? globalThis.String(object.telemetry_log_id)
        : "",
    };
  },

  toJSON(message: GetPacketFlowLogsRequest): unknown {
    const obj: any = {};
    if (message.logStreamId !== "") {
      obj.logStreamId = message.logStreamId;
    }
    if (message.from !== undefined) {
      obj.from = message.from.toISOString();
    }
    if (message.to !== undefined) {
      obj.to = message.to.toISOString();
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.nodeType !== "") {
      obj.nodeType = message.nodeType;
    }
    if (message.domainTelemetryLogId !== "") {
      obj.domainTelemetryLogId = message.domainTelemetryLogId;
    }
    if (message.telemetryLogId !== "") {
      obj.telemetryLogId = message.telemetryLogId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPacketFlowLogsRequest>, I>>(base?: I): GetPacketFlowLogsRequest {
    return GetPacketFlowLogsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPacketFlowLogsRequest>, I>>(object: I): GetPacketFlowLogsRequest {
    const message = createBaseGetPacketFlowLogsRequest();
    message.logStreamId = object.logStreamId ?? "";
    message.from = object.from ?? undefined;
    message.to = object.to ?? undefined;
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    message.nodeType = object.nodeType ?? "";
    message.domainTelemetryLogId = object.domainTelemetryLogId ?? "";
    message.telemetryLogId = object.telemetryLogId ?? "";
    return message;
  },
};

function createBaseGetPacketFlowLogsResponse(): GetPacketFlowLogsResponse {
  return { logs: [], totalCount: 0 };
}

export const GetPacketFlowLogsResponse: MessageFns<GetPacketFlowLogsResponse> = {
  encode(message: GetPacketFlowLogsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.logs) {
      StoredPacketFlowLog.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalCount !== 0) {
      writer.uint32(16).int64(message.totalCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPacketFlowLogsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPacketFlowLogsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logs.push(StoredPacketFlowLog.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalCount = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPacketFlowLogsResponse {
    return {
      logs: globalThis.Array.isArray(object?.logs) ? object.logs.map((e: any) => StoredPacketFlowLog.fromJSON(e)) : [],
      totalCount: isSet(object.totalCount)
        ? globalThis.Number(object.totalCount)
        : isSet(object.total_count)
        ? globalThis.Number(object.total_count)
        : 0,
    };
  },

  toJSON(message: GetPacketFlowLogsResponse): unknown {
    const obj: any = {};
    if (message.logs?.length) {
      obj.logs = message.logs.map((e) => StoredPacketFlowLog.toJSON(e));
    }
    if (message.totalCount !== 0) {
      obj.totalCount = Math.round(message.totalCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPacketFlowLogsResponse>, I>>(base?: I): GetPacketFlowLogsResponse {
    return GetPacketFlowLogsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPacketFlowLogsResponse>, I>>(object: I): GetPacketFlowLogsResponse {
    const message = createBaseGetPacketFlowLogsResponse();
    message.logs = object.logs?.map((e) => StoredPacketFlowLog.fromPartial(e)) || [];
    message.totalCount = object.totalCount ?? 0;
    return message;
  },
};

function createBaseStoredPacketFlowLog(): StoredPacketFlowLog {
  return {
    id: 0,
    logStreamId: "",
    nodeType: "",
    protocol: "",
    srcIp: "",
    dstIp: "",
    txPackets: 0,
    txBytes: 0,
    rxPackets: 0,
    rxBytes: 0,
    startedAt: undefined,
    endedAt: undefined,
    loggedAt: undefined,
    createdAt: undefined,
    domainTelemetryLogId: "",
    telemetryLogId: "",
    nodeName: "",
    userEmail: "",
    dstNodeName: "",
    dstUserEmail: "",
    dstNodeId: "",
  };
}

export const StoredPacketFlowLog: MessageFns<StoredPacketFlowLog> = {
  encode(message: StoredPacketFlowLog, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.logStreamId !== "") {
      writer.uint32(18).string(message.logStreamId);
    }
    if (message.nodeType !== "") {
      writer.uint32(26).string(message.nodeType);
    }
    if (message.protocol !== "") {
      writer.uint32(34).string(message.protocol);
    }
    if (message.srcIp !== "") {
      writer.uint32(42).string(message.srcIp);
    }
    if (message.dstIp !== "") {
      writer.uint32(50).string(message.dstIp);
    }
    if (message.txPackets !== 0) {
      writer.uint32(56).uint64(message.txPackets);
    }
    if (message.txBytes !== 0) {
      writer.uint32(64).uint64(message.txBytes);
    }
    if (message.rxPackets !== 0) {
      writer.uint32(72).uint64(message.rxPackets);
    }
    if (message.rxBytes !== 0) {
      writer.uint32(80).uint64(message.rxBytes);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(90).fork()).join();
    }
    if (message.endedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.endedAt), writer.uint32(98).fork()).join();
    }
    if (message.loggedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.loggedAt), writer.uint32(106).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(114).fork()).join();
    }
    if (message.domainTelemetryLogId !== "") {
      writer.uint32(122).string(message.domainTelemetryLogId);
    }
    if (message.telemetryLogId !== "") {
      writer.uint32(130).string(message.telemetryLogId);
    }
    if (message.nodeName !== "") {
      writer.uint32(138).string(message.nodeName);
    }
    if (message.userEmail !== "") {
      writer.uint32(146).string(message.userEmail);
    }
    if (message.dstNodeName !== "") {
      writer.uint32(154).string(message.dstNodeName);
    }
    if (message.dstUserEmail !== "") {
      writer.uint32(162).string(message.dstUserEmail);
    }
    if (message.dstNodeId !== "") {
      writer.uint32(170).string(message.dstNodeId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredPacketFlowLog {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredPacketFlowLog();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.logStreamId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nodeType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.protocol = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.srcIp = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.dstIp = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.txPackets = longToNumber(reader.uint64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.txBytes = longToNumber(reader.uint64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.rxPackets = longToNumber(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.rxBytes = longToNumber(reader.uint64());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.endedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.loggedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.domainTelemetryLogId = reader.string();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.telemetryLogId = reader.string();
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.nodeName = reader.string();
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.userEmail = reader.string();
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.dstNodeName = reader.string();
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.dstUserEmail = reader.string();
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.dstNodeId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredPacketFlowLog {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      logStreamId: isSet(object.logStreamId)
        ? globalThis.String(object.logStreamId)
        : isSet(object.log_stream_id)
        ? globalThis.String(object.log_stream_id)
        : "",
      nodeType: isSet(object.nodeType)
        ? globalThis.String(object.nodeType)
        : isSet(object.node_type)
        ? globalThis.String(object.node_type)
        : "",
      protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : "",
      srcIp: isSet(object.srcIp)
        ? globalThis.String(object.srcIp)
        : isSet(object.src_ip)
        ? globalThis.String(object.src_ip)
        : "",
      dstIp: isSet(object.dstIp)
        ? globalThis.String(object.dstIp)
        : isSet(object.dst_ip)
        ? globalThis.String(object.dst_ip)
        : "",
      txPackets: isSet(object.txPackets)
        ? globalThis.Number(object.txPackets)
        : isSet(object.tx_packets)
        ? globalThis.Number(object.tx_packets)
        : 0,
      txBytes: isSet(object.txBytes)
        ? globalThis.Number(object.txBytes)
        : isSet(object.tx_bytes)
        ? globalThis.Number(object.tx_bytes)
        : 0,
      rxPackets: isSet(object.rxPackets)
        ? globalThis.Number(object.rxPackets)
        : isSet(object.rx_packets)
        ? globalThis.Number(object.rx_packets)
        : 0,
      rxBytes: isSet(object.rxBytes)
        ? globalThis.Number(object.rxBytes)
        : isSet(object.rx_bytes)
        ? globalThis.Number(object.rx_bytes)
        : 0,
      startedAt: isSet(object.startedAt)
        ? fromJsonTimestamp(object.startedAt)
        : isSet(object.started_at)
        ? fromJsonTimestamp(object.started_at)
        : undefined,
      endedAt: isSet(object.endedAt)
        ? fromJsonTimestamp(object.endedAt)
        : isSet(object.ended_at)
        ? fromJsonTimestamp(object.ended_at)
        : undefined,
      loggedAt: isSet(object.loggedAt)
        ? fromJsonTimestamp(object.loggedAt)
        : isSet(object.logged_at)
        ? fromJsonTimestamp(object.logged_at)
        : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      domainTelemetryLogId: isSet(object.domainTelemetryLogId)
        ? globalThis.String(object.domainTelemetryLogId)
        : isSet(object.domain_telemetry_log_id)
        ? globalThis.String(object.domain_telemetry_log_id)
        : "",
      telemetryLogId: isSet(object.telemetryLogId)
        ? globalThis.String(object.telemetryLogId)
        : isSet(object.telemetry_log_id)
        ? globalThis.String(object.telemetry_log_id)
        : "",
      nodeName: isSet(object.nodeName)
        ? globalThis.String(object.nodeName)
        : isSet(object.node_name)
        ? globalThis.String(object.node_name)
        : "",
      userEmail: isSet(object.userEmail)
        ? globalThis.String(object.userEmail)
        : isSet(object.user_email)
        ? globalThis.String(object.user_email)
        : "",
      dstNodeName: isSet(object.dstNodeName)
        ? globalThis.String(object.dstNodeName)
        : isSet(object.dst_node_name)
        ? globalThis.String(object.dst_node_name)
        : "",
      dstUserEmail: isSet(object.dstUserEmail)
        ? globalThis.String(object.dstUserEmail)
        : isSet(object.dst_user_email)
        ? globalThis.String(object.dst_user_email)
        : "",
      dstNodeId: isSet(object.dstNodeId)
        ? globalThis.String(object.dstNodeId)
        : isSet(object.dst_node_id)
        ? globalThis.String(object.dst_node_id)
        : "",
    };
  },

  toJSON(message: StoredPacketFlowLog): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.logStreamId !== "") {
      obj.logStreamId = message.logStreamId;
    }
    if (message.nodeType !== "") {
      obj.nodeType = message.nodeType;
    }
    if (message.protocol !== "") {
      obj.protocol = message.protocol;
    }
    if (message.srcIp !== "") {
      obj.srcIp = message.srcIp;
    }
    if (message.dstIp !== "") {
      obj.dstIp = message.dstIp;
    }
    if (message.txPackets !== 0) {
      obj.txPackets = Math.round(message.txPackets);
    }
    if (message.txBytes !== 0) {
      obj.txBytes = Math.round(message.txBytes);
    }
    if (message.rxPackets !== 0) {
      obj.rxPackets = Math.round(message.rxPackets);
    }
    if (message.rxBytes !== 0) {
      obj.rxBytes = Math.round(message.rxBytes);
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.endedAt !== undefined) {
      obj.endedAt = message.endedAt.toISOString();
    }
    if (message.loggedAt !== undefined) {
      obj.loggedAt = message.loggedAt.toISOString();
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.domainTelemetryLogId !== "") {
      obj.domainTelemetryLogId = message.domainTelemetryLogId;
    }
    if (message.telemetryLogId !== "") {
      obj.telemetryLogId = message.telemetryLogId;
    }
    if (message.nodeName !== "") {
      obj.nodeName = message.nodeName;
    }
    if (message.userEmail !== "") {
      obj.userEmail = message.userEmail;
    }
    if (message.dstNodeName !== "") {
      obj.dstNodeName = message.dstNodeName;
    }
    if (message.dstUserEmail !== "") {
      obj.dstUserEmail = message.dstUserEmail;
    }
    if (message.dstNodeId !== "") {
      obj.dstNodeId = message.dstNodeId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredPacketFlowLog>, I>>(base?: I): StoredPacketFlowLog {
    return StoredPacketFlowLog.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredPacketFlowLog>, I>>(object: I): StoredPacketFlowLog {
    const message = createBaseStoredPacketFlowLog();
    message.id = object.id ?? 0;
    message.logStreamId = object.logStreamId ?? "";
    message.nodeType = object.nodeType ?? "";
    message.protocol = object.protocol ?? "";
    message.srcIp = object.srcIp ?? "";
    message.dstIp = object.dstIp ?? "";
    message.txPackets = object.txPackets ?? 0;
    message.txBytes = object.txBytes ?? 0;
    message.rxPackets = object.rxPackets ?? 0;
    message.rxBytes = object.rxBytes ?? 0;
    message.startedAt = object.startedAt ?? undefined;
    message.endedAt = object.endedAt ?? undefined;
    message.loggedAt = object.loggedAt ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.domainTelemetryLogId = object.domainTelemetryLogId ?? "";
    message.telemetryLogId = object.telemetryLogId ?? "";
    message.nodeName = object.nodeName ?? "";
    message.userEmail = object.userEmail ?? "";
    message.dstNodeName = object.dstNodeName ?? "";
    message.dstUserEmail = object.dstUserEmail ?? "";
    message.dstNodeId = object.dstNodeId ?? "";
    return message;
  },
};

/**
 * LogReaderService provides log query APIs for administrators and internal services.
 * Authentication: gRPC metadata "x-api-key" header.
 * Queries are based on log_stream_id (PublicID = SHA-256 of PrivateID).
 */
export interface LogReaderService {
  /** GetLoglyphEntries retrieves client debug log entries. */
  GetLoglyphEntries(request: DeepPartial<GetLoglyphRequest>, metadata?: grpc.Metadata): Promise<GetLoglyphResponse>;
  /** GetOrbitEvents retrieves stored telemetry events. */
  GetOrbitEvents(
    request: DeepPartial<GetOrbitEventsRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetOrbitEventsResponse>;
  /** GetOrbitDailyCounts retrieves aggregated daily telemetry counts. */
  GetOrbitDailyCounts(
    request: DeepPartial<GetOrbitDailyCountsRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetOrbitDailyCountsResponse>;
  /** GetPacketFlowLogs retrieves network flow statistics. */
  GetPacketFlowLogs(
    request: DeepPartial<GetPacketFlowLogsRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetPacketFlowLogsResponse>;
}

export class LogReaderServiceClientImpl implements LogReaderService {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.GetLoglyphEntries = this.GetLoglyphEntries.bind(this);
    this.GetOrbitEvents = this.GetOrbitEvents.bind(this);
    this.GetOrbitDailyCounts = this.GetOrbitDailyCounts.bind(this);
    this.GetPacketFlowLogs = this.GetPacketFlowLogs.bind(this);
  }

  GetLoglyphEntries(request: DeepPartial<GetLoglyphRequest>, metadata?: grpc.Metadata): Promise<GetLoglyphResponse> {
    return this.rpc.unary(LogReaderServiceGetLoglyphEntriesDesc, GetLoglyphRequest.fromPartial(request), metadata);
  }

  GetOrbitEvents(
    request: DeepPartial<GetOrbitEventsRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetOrbitEventsResponse> {
    return this.rpc.unary(LogReaderServiceGetOrbitEventsDesc, GetOrbitEventsRequest.fromPartial(request), metadata);
  }

  GetOrbitDailyCounts(
    request: DeepPartial<GetOrbitDailyCountsRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetOrbitDailyCountsResponse> {
    return this.rpc.unary(
      LogReaderServiceGetOrbitDailyCountsDesc,
      GetOrbitDailyCountsRequest.fromPartial(request),
      metadata,
    );
  }

  GetPacketFlowLogs(
    request: DeepPartial<GetPacketFlowLogsRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetPacketFlowLogsResponse> {
    return this.rpc.unary(
      LogReaderServiceGetPacketFlowLogsDesc,
      GetPacketFlowLogsRequest.fromPartial(request),
      metadata,
    );
  }
}

export const LogReaderServiceDesc = { serviceName: "logserver.LogReaderService" };

export const LogReaderServiceGetLoglyphEntriesDesc: UnaryMethodDefinitionish = {
  methodName: "GetLoglyphEntries",
  service: LogReaderServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return GetLoglyphRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = GetLoglyphResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const LogReaderServiceGetOrbitEventsDesc: UnaryMethodDefinitionish = {
  methodName: "GetOrbitEvents",
  service: LogReaderServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return GetOrbitEventsRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = GetOrbitEventsResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const LogReaderServiceGetOrbitDailyCountsDesc: UnaryMethodDefinitionish = {
  methodName: "GetOrbitDailyCounts",
  service: LogReaderServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return GetOrbitDailyCountsRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = GetOrbitDailyCountsResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const LogReaderServiceGetPacketFlowLogsDesc: UnaryMethodDefinitionish = {
  methodName: "GetPacketFlowLogs",
  service: LogReaderServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return GetPacketFlowLogsRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = GetPacketFlowLogsResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
