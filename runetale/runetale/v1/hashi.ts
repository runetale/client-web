// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               v3.20.3
// source: runetale/runetale/v1/hashi.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import { Empty } from "../../../google/protobuf/empty";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "protos";

export interface Endpoint {
  /** net.UDPAddrを文字列で表現 (e.g., "192.168.1.1:51820") */
  addr: string;
  /** "turn" or "stun" from ice_endpoint.go */
  type: string;
}

export interface HashiStatus {
  backendState: string;
  /** netip.Addr を文字列として扱う */
  runetaleIps: string[];
  interactiveLoginUrl: string;
  self?:
    | PeerStatus
    | undefined;
  /** map[key.NodePublic]*PeerStatus */
  peer: { [key: string]: PeerStatus };
}

export interface HashiStatus_PeerEntry {
  key: string;
  value: PeerStatus | undefined;
}

export interface PeerStatus {
  id: number;
  publicKey: string;
  os: string;
  hostName: string;
  /** netip.Addr を文字列として扱う */
  runetaleIps: string[];
  peerApiUrl: string[];
  /** [netip.Prefix] を文字列のリストで表現 */
  allowedIps: string[];
  rxBytes: number;
  txBytes: number;
  lastHandshake: Date | undefined;
  lastWrite: Date | undefined;
  addrs: string[];
  curAddr?: string | undefined;
  iceAddr?: string | undefined;
  advertiseRoutes: string[];
  /** subnet linkerを許可するかどうか */
  isSnatSubnet: boolean;
  /** active: 最後のパケット送信から45秒以内か（wonderwallで設定） */
  active: boolean;
  /** online: サーバーに接続しているか (wonderwallで設定) */
  online: boolean;
  /** last_seen: 最後にオンラインだった時刻（オフライン時のみ設定） */
  lastSeen: Date | undefined;
}

export interface UserspacePeerEngineStatus {
  gotAt: Date | undefined;
  peers: CompactPeerStatus[];
  /** []*runecfg.Endpoint */
  localAddrs: Endpoint[];
}

export interface CompactPeerStatus {
  nodeKey: string;
  txBytes: number;
  rxBytes: number;
  lastHandshake: Date | undefined;
}

export interface PingResult {
  ip: string;
  nodeIp: string;
  nodeName: string;
  err: string;
  latencySeconds: number;
  endpoint: string;
  peerApiPort: number;
  peerApiUrl: string;
  isLocalIp: boolean;
}

export interface ComposeRequest {
  key: string;
}

export interface PingRequest {
  ip: string;
  type: string;
  size: number;
}

export interface StopRequest {
  reason: string;
}

export interface NetCheckReport {
  now:
    | Date
    | undefined;
  /** a UDP STUN round trip completed */
  udp: boolean;
  /** an IPv6 STUN round trip completed */
  ipv6: boolean;
  /** an IPv4 STUN round trip completed */
  ipv4: boolean;
  /** an IPv6 packet was able to be sent */
  ipv6CanSend: boolean;
  /** an IPv4 packet was able to be sent */
  ipv4CanSend: boolean;
  /** could bind a socket to ::1 */
  osHasIpv6: boolean;
  /** best IPv4 global address (netip.AddrPort as string) */
  globalV4: string;
  /** best IPv6 global address (netip.AddrPort as string) */
  globalV6: string;
  /** STUN server URI -> latency in milliseconds */
  stunLatency: { [key: string]: number };
  /** TURN server URI -> latency in milliseconds */
  turnLatency: { [key: string]: number };
  /** errors encountered during the check */
  errors: string[];
  /** CERF relay latency per region. Key is "regionCode" (e.g. "tokyo"), value is latency in milliseconds. */
  cerfLatency: { [key: string]: number };
  /** preferred_cerf_region is the region code of the nearest (lowest latency) CERF relay. */
  preferredCerfRegion: string;
  /** preferred_cerf_region_id is the region ID of the nearest CERF relay. */
  preferredCerfRegionId: number;
}

export interface NetCheckReport_StunLatencyEntry {
  key: string;
  value: number;
}

export interface NetCheckReport_TurnLatencyEntry {
  key: string;
  value: number;
}

export interface NetCheckReport_CerfLatencyEntry {
  key: string;
  value: number;
}

function createBaseEndpoint(): Endpoint {
  return { addr: "", type: "" };
}

export const Endpoint: MessageFns<Endpoint> = {
  encode(message: Endpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.addr !== "") {
      writer.uint32(10).string(message.addr);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Endpoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEndpoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.addr = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Endpoint {
    return {
      addr: isSet(object.addr) ? globalThis.String(object.addr) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
    };
  },

  toJSON(message: Endpoint): unknown {
    const obj: any = {};
    if (message.addr !== "") {
      obj.addr = message.addr;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Endpoint>, I>>(base?: I): Endpoint {
    return Endpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Endpoint>, I>>(object: I): Endpoint {
    const message = createBaseEndpoint();
    message.addr = object.addr ?? "";
    message.type = object.type ?? "";
    return message;
  },
};

function createBaseHashiStatus(): HashiStatus {
  return { backendState: "", runetaleIps: [], interactiveLoginUrl: "", self: undefined, peer: {} };
}

export const HashiStatus: MessageFns<HashiStatus> = {
  encode(message: HashiStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.backendState !== "") {
      writer.uint32(10).string(message.backendState);
    }
    for (const v of message.runetaleIps) {
      writer.uint32(18).string(v!);
    }
    if (message.interactiveLoginUrl !== "") {
      writer.uint32(26).string(message.interactiveLoginUrl);
    }
    if (message.self !== undefined) {
      PeerStatus.encode(message.self, writer.uint32(34).fork()).join();
    }
    globalThis.Object.entries(message.peer).forEach(([key, value]: [string, PeerStatus]) => {
      HashiStatus_PeerEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HashiStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHashiStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.backendState = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.runetaleIps.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.interactiveLoginUrl = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.self = PeerStatus.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = HashiStatus_PeerEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.peer[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HashiStatus {
    return {
      backendState: isSet(object.backendState)
        ? globalThis.String(object.backendState)
        : isSet(object.backend_state)
        ? globalThis.String(object.backend_state)
        : "",
      runetaleIps: globalThis.Array.isArray(object?.runetaleIps)
        ? object.runetaleIps.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.runetale_ips)
        ? object.runetale_ips.map((e: any) => globalThis.String(e))
        : [],
      interactiveLoginUrl: isSet(object.interactiveLoginUrl)
        ? globalThis.String(object.interactiveLoginUrl)
        : isSet(object.interactive_login_url)
        ? globalThis.String(object.interactive_login_url)
        : "",
      self: isSet(object.self) ? PeerStatus.fromJSON(object.self) : undefined,
      peer: isObject(object.peer)
        ? (globalThis.Object.entries(object.peer) as [string, any][]).reduce(
          (acc: { [key: string]: PeerStatus }, [key, value]: [string, any]) => {
            acc[key] = PeerStatus.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: HashiStatus): unknown {
    const obj: any = {};
    if (message.backendState !== "") {
      obj.backendState = message.backendState;
    }
    if (message.runetaleIps?.length) {
      obj.runetaleIps = message.runetaleIps;
    }
    if (message.interactiveLoginUrl !== "") {
      obj.interactiveLoginUrl = message.interactiveLoginUrl;
    }
    if (message.self !== undefined) {
      obj.self = PeerStatus.toJSON(message.self);
    }
    if (message.peer) {
      const entries = globalThis.Object.entries(message.peer) as [string, PeerStatus][];
      if (entries.length > 0) {
        obj.peer = {};
        entries.forEach(([k, v]) => {
          obj.peer[k] = PeerStatus.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HashiStatus>, I>>(base?: I): HashiStatus {
    return HashiStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HashiStatus>, I>>(object: I): HashiStatus {
    const message = createBaseHashiStatus();
    message.backendState = object.backendState ?? "";
    message.runetaleIps = object.runetaleIps?.map((e) => e) || [];
    message.interactiveLoginUrl = object.interactiveLoginUrl ?? "";
    message.self = (object.self !== undefined && object.self !== null)
      ? PeerStatus.fromPartial(object.self)
      : undefined;
    message.peer = (globalThis.Object.entries(object.peer ?? {}) as [string, PeerStatus][]).reduce(
      (acc: { [key: string]: PeerStatus }, [key, value]: [string, PeerStatus]) => {
        if (value !== undefined) {
          acc[key] = PeerStatus.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseHashiStatus_PeerEntry(): HashiStatus_PeerEntry {
  return { key: "", value: undefined };
}

export const HashiStatus_PeerEntry: MessageFns<HashiStatus_PeerEntry> = {
  encode(message: HashiStatus_PeerEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      PeerStatus.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HashiStatus_PeerEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHashiStatus_PeerEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = PeerStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HashiStatus_PeerEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? PeerStatus.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: HashiStatus_PeerEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = PeerStatus.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HashiStatus_PeerEntry>, I>>(base?: I): HashiStatus_PeerEntry {
    return HashiStatus_PeerEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HashiStatus_PeerEntry>, I>>(object: I): HashiStatus_PeerEntry {
    const message = createBaseHashiStatus_PeerEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? PeerStatus.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBasePeerStatus(): PeerStatus {
  return {
    id: 0,
    publicKey: "",
    os: "",
    hostName: "",
    runetaleIps: [],
    peerApiUrl: [],
    allowedIps: [],
    rxBytes: 0,
    txBytes: 0,
    lastHandshake: undefined,
    lastWrite: undefined,
    addrs: [],
    curAddr: undefined,
    iceAddr: undefined,
    advertiseRoutes: [],
    isSnatSubnet: false,
    active: false,
    online: false,
    lastSeen: undefined,
  };
}

export const PeerStatus: MessageFns<PeerStatus> = {
  encode(message: PeerStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int64(message.id);
    }
    if (message.publicKey !== "") {
      writer.uint32(18).string(message.publicKey);
    }
    if (message.os !== "") {
      writer.uint32(26).string(message.os);
    }
    if (message.hostName !== "") {
      writer.uint32(34).string(message.hostName);
    }
    for (const v of message.runetaleIps) {
      writer.uint32(42).string(v!);
    }
    for (const v of message.peerApiUrl) {
      writer.uint32(50).string(v!);
    }
    for (const v of message.allowedIps) {
      writer.uint32(58).string(v!);
    }
    if (message.rxBytes !== 0) {
      writer.uint32(64).int64(message.rxBytes);
    }
    if (message.txBytes !== 0) {
      writer.uint32(72).int64(message.txBytes);
    }
    if (message.lastHandshake !== undefined) {
      Timestamp.encode(toTimestamp(message.lastHandshake), writer.uint32(82).fork()).join();
    }
    if (message.lastWrite !== undefined) {
      Timestamp.encode(toTimestamp(message.lastWrite), writer.uint32(90).fork()).join();
    }
    for (const v of message.addrs) {
      writer.uint32(98).string(v!);
    }
    if (message.curAddr !== undefined) {
      writer.uint32(106).string(message.curAddr);
    }
    if (message.iceAddr !== undefined) {
      writer.uint32(114).string(message.iceAddr);
    }
    for (const v of message.advertiseRoutes) {
      writer.uint32(122).string(v!);
    }
    if (message.isSnatSubnet !== false) {
      writer.uint32(128).bool(message.isSnatSubnet);
    }
    if (message.active !== false) {
      writer.uint32(136).bool(message.active);
    }
    if (message.online !== false) {
      writer.uint32(144).bool(message.online);
    }
    if (message.lastSeen !== undefined) {
      Timestamp.encode(toTimestamp(message.lastSeen), writer.uint32(154).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PeerStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePeerStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.publicKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.os = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.hostName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.runetaleIps.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.peerApiUrl.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.allowedIps.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.rxBytes = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.txBytes = longToNumber(reader.int64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.lastHandshake = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.lastWrite = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.addrs.push(reader.string());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.curAddr = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.iceAddr = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.advertiseRoutes.push(reader.string());
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.isSnatSubnet = reader.bool();
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.active = reader.bool();
          continue;
        }
        case 18: {
          if (tag !== 144) {
            break;
          }

          message.online = reader.bool();
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.lastSeen = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PeerStatus {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      publicKey: isSet(object.publicKey)
        ? globalThis.String(object.publicKey)
        : isSet(object.public_key)
        ? globalThis.String(object.public_key)
        : "",
      os: isSet(object.os) ? globalThis.String(object.os) : "",
      hostName: isSet(object.hostName)
        ? globalThis.String(object.hostName)
        : isSet(object.host_name)
        ? globalThis.String(object.host_name)
        : "",
      runetaleIps: globalThis.Array.isArray(object?.RunetaleIPs)
        ? object.RunetaleIPs.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.runetale_ips)
        ? object.runetale_ips.map((e: any) => globalThis.String(e))
        : [],
      peerApiUrl: globalThis.Array.isArray(object?.peerApiUrl)
        ? object.peerApiUrl.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.peer_api_url)
        ? object.peer_api_url.map((e: any) => globalThis.String(e))
        : [],
      allowedIps: globalThis.Array.isArray(object?.allowedIps)
        ? object.allowedIps.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.allowed_ips)
        ? object.allowed_ips.map((e: any) => globalThis.String(e))
        : [],
      rxBytes: isSet(object.rxBytes)
        ? globalThis.Number(object.rxBytes)
        : isSet(object.rx_bytes)
        ? globalThis.Number(object.rx_bytes)
        : 0,
      txBytes: isSet(object.txBytes)
        ? globalThis.Number(object.txBytes)
        : isSet(object.tx_bytes)
        ? globalThis.Number(object.tx_bytes)
        : 0,
      lastHandshake: isSet(object.lastHandshake)
        ? fromJsonTimestamp(object.lastHandshake)
        : isSet(object.last_handshake)
        ? fromJsonTimestamp(object.last_handshake)
        : undefined,
      lastWrite: isSet(object.lastWrite)
        ? fromJsonTimestamp(object.lastWrite)
        : isSet(object.last_write)
        ? fromJsonTimestamp(object.last_write)
        : undefined,
      addrs: globalThis.Array.isArray(object?.addrs)
        ? object.addrs.map((e: any) => globalThis.String(e))
        : [],
      curAddr: isSet(object.curAddr)
        ? globalThis.String(object.curAddr)
        : isSet(object.cur_addr)
        ? globalThis.String(object.cur_addr)
        : undefined,
      iceAddr: isSet(object.iceAddr)
        ? globalThis.String(object.iceAddr)
        : isSet(object.ice_addr)
        ? globalThis.String(object.ice_addr)
        : undefined,
      advertiseRoutes: globalThis.Array.isArray(object?.advertiseRoutes)
        ? object.advertiseRoutes.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.advertise_routes)
        ? object.advertise_routes.map((e: any) => globalThis.String(e))
        : [],
      isSnatSubnet: isSet(object.isSnatSubnet)
        ? globalThis.Boolean(object.isSnatSubnet)
        : isSet(object.is_snat_subnet)
        ? globalThis.Boolean(object.is_snat_subnet)
        : false,
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
      online: isSet(object.online) ? globalThis.Boolean(object.online) : false,
      lastSeen: isSet(object.lastSeen)
        ? fromJsonTimestamp(object.lastSeen)
        : isSet(object.last_seen)
        ? fromJsonTimestamp(object.last_seen)
        : undefined,
    };
  },

  toJSON(message: PeerStatus): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.publicKey !== "") {
      obj.publicKey = message.publicKey;
    }
    if (message.os !== "") {
      obj.os = message.os;
    }
    if (message.hostName !== "") {
      obj.hostName = message.hostName;
    }
    if (message.runetaleIps?.length) {
      obj.RunetaleIPs = message.runetaleIps;
    }
    if (message.peerApiUrl?.length) {
      obj.peerApiUrl = message.peerApiUrl;
    }
    if (message.allowedIps?.length) {
      obj.allowedIps = message.allowedIps;
    }
    if (message.rxBytes !== 0) {
      obj.rxBytes = Math.round(message.rxBytes);
    }
    if (message.txBytes !== 0) {
      obj.txBytes = Math.round(message.txBytes);
    }
    if (message.lastHandshake !== undefined) {
      obj.lastHandshake = message.lastHandshake.toISOString();
    }
    if (message.lastWrite !== undefined) {
      obj.lastWrite = message.lastWrite.toISOString();
    }
    if (message.addrs?.length) {
      obj.addrs = message.addrs;
    }
    if (message.curAddr !== undefined) {
      obj.curAddr = message.curAddr;
    }
    if (message.iceAddr !== undefined) {
      obj.iceAddr = message.iceAddr;
    }
    if (message.advertiseRoutes?.length) {
      obj.advertiseRoutes = message.advertiseRoutes;
    }
    if (message.isSnatSubnet !== false) {
      obj.isSnatSubnet = message.isSnatSubnet;
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    if (message.online !== false) {
      obj.online = message.online;
    }
    if (message.lastSeen !== undefined) {
      obj.lastSeen = message.lastSeen.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PeerStatus>, I>>(base?: I): PeerStatus {
    return PeerStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PeerStatus>, I>>(object: I): PeerStatus {
    const message = createBasePeerStatus();
    message.id = object.id ?? 0;
    message.publicKey = object.publicKey ?? "";
    message.os = object.os ?? "";
    message.hostName = object.hostName ?? "";
    message.runetaleIps = object.runetaleIps?.map((e) => e) || [];
    message.peerApiUrl = object.peerApiUrl?.map((e) => e) || [];
    message.allowedIps = object.allowedIps?.map((e) => e) || [];
    message.rxBytes = object.rxBytes ?? 0;
    message.txBytes = object.txBytes ?? 0;
    message.lastHandshake = object.lastHandshake ?? undefined;
    message.lastWrite = object.lastWrite ?? undefined;
    message.addrs = object.addrs?.map((e) => e) || [];
    message.curAddr = object.curAddr ?? undefined;
    message.iceAddr = object.iceAddr ?? undefined;
    message.advertiseRoutes = object.advertiseRoutes?.map((e) => e) || [];
    message.isSnatSubnet = object.isSnatSubnet ?? false;
    message.active = object.active ?? false;
    message.online = object.online ?? false;
    message.lastSeen = object.lastSeen ?? undefined;
    return message;
  },
};

function createBaseUserspacePeerEngineStatus(): UserspacePeerEngineStatus {
  return { gotAt: undefined, peers: [], localAddrs: [] };
}

export const UserspacePeerEngineStatus: MessageFns<UserspacePeerEngineStatus> = {
  encode(message: UserspacePeerEngineStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gotAt !== undefined) {
      Timestamp.encode(toTimestamp(message.gotAt), writer.uint32(10).fork()).join();
    }
    for (const v of message.peers) {
      CompactPeerStatus.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.localAddrs) {
      Endpoint.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserspacePeerEngineStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserspacePeerEngineStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gotAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.peers.push(CompactPeerStatus.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.localAddrs.push(Endpoint.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserspacePeerEngineStatus {
    return {
      gotAt: isSet(object.gotAt)
        ? fromJsonTimestamp(object.gotAt)
        : isSet(object.got_at)
        ? fromJsonTimestamp(object.got_at)
        : undefined,
      peers: globalThis.Array.isArray(object?.peers) ? object.peers.map((e: any) => CompactPeerStatus.fromJSON(e)) : [],
      localAddrs: globalThis.Array.isArray(object?.localAddrs)
        ? object.localAddrs.map((e: any) => Endpoint.fromJSON(e))
        : globalThis.Array.isArray(object?.local_addrs)
        ? object.local_addrs.map((e: any) => Endpoint.fromJSON(e))
        : [],
    };
  },

  toJSON(message: UserspacePeerEngineStatus): unknown {
    const obj: any = {};
    if (message.gotAt !== undefined) {
      obj.gotAt = message.gotAt.toISOString();
    }
    if (message.peers?.length) {
      obj.peers = message.peers.map((e) => CompactPeerStatus.toJSON(e));
    }
    if (message.localAddrs?.length) {
      obj.localAddrs = message.localAddrs.map((e) => Endpoint.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserspacePeerEngineStatus>, I>>(base?: I): UserspacePeerEngineStatus {
    return UserspacePeerEngineStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserspacePeerEngineStatus>, I>>(object: I): UserspacePeerEngineStatus {
    const message = createBaseUserspacePeerEngineStatus();
    message.gotAt = object.gotAt ?? undefined;
    message.peers = object.peers?.map((e) => CompactPeerStatus.fromPartial(e)) || [];
    message.localAddrs = object.localAddrs?.map((e) => Endpoint.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCompactPeerStatus(): CompactPeerStatus {
  return { nodeKey: "", txBytes: 0, rxBytes: 0, lastHandshake: undefined };
}

export const CompactPeerStatus: MessageFns<CompactPeerStatus> = {
  encode(message: CompactPeerStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeKey !== "") {
      writer.uint32(10).string(message.nodeKey);
    }
    if (message.txBytes !== 0) {
      writer.uint32(16).int64(message.txBytes);
    }
    if (message.rxBytes !== 0) {
      writer.uint32(24).int64(message.rxBytes);
    }
    if (message.lastHandshake !== undefined) {
      Timestamp.encode(toTimestamp(message.lastHandshake), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CompactPeerStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCompactPeerStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nodeKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.txBytes = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.rxBytes = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.lastHandshake = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CompactPeerStatus {
    return {
      nodeKey: isSet(object.nodeKey)
        ? globalThis.String(object.nodeKey)
        : isSet(object.node_key)
        ? globalThis.String(object.node_key)
        : "",
      txBytes: isSet(object.txBytes)
        ? globalThis.Number(object.txBytes)
        : isSet(object.tx_bytes)
        ? globalThis.Number(object.tx_bytes)
        : 0,
      rxBytes: isSet(object.rxBytes)
        ? globalThis.Number(object.rxBytes)
        : isSet(object.rx_bytes)
        ? globalThis.Number(object.rx_bytes)
        : 0,
      lastHandshake: isSet(object.lastHandshake)
        ? fromJsonTimestamp(object.lastHandshake)
        : isSet(object.last_handshake)
        ? fromJsonTimestamp(object.last_handshake)
        : undefined,
    };
  },

  toJSON(message: CompactPeerStatus): unknown {
    const obj: any = {};
    if (message.nodeKey !== "") {
      obj.nodeKey = message.nodeKey;
    }
    if (message.txBytes !== 0) {
      obj.txBytes = Math.round(message.txBytes);
    }
    if (message.rxBytes !== 0) {
      obj.rxBytes = Math.round(message.rxBytes);
    }
    if (message.lastHandshake !== undefined) {
      obj.lastHandshake = message.lastHandshake.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CompactPeerStatus>, I>>(base?: I): CompactPeerStatus {
    return CompactPeerStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CompactPeerStatus>, I>>(object: I): CompactPeerStatus {
    const message = createBaseCompactPeerStatus();
    message.nodeKey = object.nodeKey ?? "";
    message.txBytes = object.txBytes ?? 0;
    message.rxBytes = object.rxBytes ?? 0;
    message.lastHandshake = object.lastHandshake ?? undefined;
    return message;
  },
};

function createBasePingResult(): PingResult {
  return {
    ip: "",
    nodeIp: "",
    nodeName: "",
    err: "",
    latencySeconds: 0,
    endpoint: "",
    peerApiPort: 0,
    peerApiUrl: "",
    isLocalIp: false,
  };
}

export const PingResult: MessageFns<PingResult> = {
  encode(message: PingResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ip !== "") {
      writer.uint32(10).string(message.ip);
    }
    if (message.nodeIp !== "") {
      writer.uint32(18).string(message.nodeIp);
    }
    if (message.nodeName !== "") {
      writer.uint32(26).string(message.nodeName);
    }
    if (message.err !== "") {
      writer.uint32(34).string(message.err);
    }
    if (message.latencySeconds !== 0) {
      writer.uint32(41).double(message.latencySeconds);
    }
    if (message.endpoint !== "") {
      writer.uint32(50).string(message.endpoint);
    }
    if (message.peerApiPort !== 0) {
      writer.uint32(56).uint32(message.peerApiPort);
    }
    if (message.peerApiUrl !== "") {
      writer.uint32(66).string(message.peerApiUrl);
    }
    if (message.isLocalIp !== false) {
      writer.uint32(72).bool(message.isLocalIp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PingResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ip = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodeIp = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nodeName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.err = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.latencySeconds = reader.double();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.endpoint = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.peerApiPort = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.peerApiUrl = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.isLocalIp = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PingResult {
    return {
      ip: isSet(object.ip) ? globalThis.String(object.ip) : "",
      nodeIp: isSet(object.nodeIp)
        ? globalThis.String(object.nodeIp)
        : isSet(object.node_ip)
        ? globalThis.String(object.node_ip)
        : "",
      nodeName: isSet(object.nodeName)
        ? globalThis.String(object.nodeName)
        : isSet(object.node_name)
        ? globalThis.String(object.node_name)
        : "",
      err: isSet(object.err) ? globalThis.String(object.err) : "",
      latencySeconds: isSet(object.latencySeconds)
        ? globalThis.Number(object.latencySeconds)
        : isSet(object.latency_seconds)
        ? globalThis.Number(object.latency_seconds)
        : 0,
      endpoint: isSet(object.endpoint) ? globalThis.String(object.endpoint) : "",
      peerApiPort: isSet(object.peerApiPort)
        ? globalThis.Number(object.peerApiPort)
        : isSet(object.peer_api_port)
        ? globalThis.Number(object.peer_api_port)
        : 0,
      peerApiUrl: isSet(object.peerApiUrl)
        ? globalThis.String(object.peerApiUrl)
        : isSet(object.peer_api_url)
        ? globalThis.String(object.peer_api_url)
        : "",
      isLocalIp: isSet(object.isLocalIp)
        ? globalThis.Boolean(object.isLocalIp)
        : isSet(object.is_local_ip)
        ? globalThis.Boolean(object.is_local_ip)
        : false,
    };
  },

  toJSON(message: PingResult): unknown {
    const obj: any = {};
    if (message.ip !== "") {
      obj.ip = message.ip;
    }
    if (message.nodeIp !== "") {
      obj.nodeIp = message.nodeIp;
    }
    if (message.nodeName !== "") {
      obj.nodeName = message.nodeName;
    }
    if (message.err !== "") {
      obj.err = message.err;
    }
    if (message.latencySeconds !== 0) {
      obj.latencySeconds = message.latencySeconds;
    }
    if (message.endpoint !== "") {
      obj.endpoint = message.endpoint;
    }
    if (message.peerApiPort !== 0) {
      obj.peerApiPort = Math.round(message.peerApiPort);
    }
    if (message.peerApiUrl !== "") {
      obj.peerApiUrl = message.peerApiUrl;
    }
    if (message.isLocalIp !== false) {
      obj.isLocalIp = message.isLocalIp;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PingResult>, I>>(base?: I): PingResult {
    return PingResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PingResult>, I>>(object: I): PingResult {
    const message = createBasePingResult();
    message.ip = object.ip ?? "";
    message.nodeIp = object.nodeIp ?? "";
    message.nodeName = object.nodeName ?? "";
    message.err = object.err ?? "";
    message.latencySeconds = object.latencySeconds ?? 0;
    message.endpoint = object.endpoint ?? "";
    message.peerApiPort = object.peerApiPort ?? 0;
    message.peerApiUrl = object.peerApiUrl ?? "";
    message.isLocalIp = object.isLocalIp ?? false;
    return message;
  },
};

function createBaseComposeRequest(): ComposeRequest {
  return { key: "" };
}

export const ComposeRequest: MessageFns<ComposeRequest> = {
  encode(message: ComposeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComposeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComposeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComposeRequest {
    return { key: isSet(object.key) ? globalThis.String(object.key) : "" };
  },

  toJSON(message: ComposeRequest): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComposeRequest>, I>>(base?: I): ComposeRequest {
    return ComposeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComposeRequest>, I>>(object: I): ComposeRequest {
    const message = createBaseComposeRequest();
    message.key = object.key ?? "";
    return message;
  },
};

function createBasePingRequest(): PingRequest {
  return { ip: "", type: "", size: 0 };
}

export const PingRequest: MessageFns<PingRequest> = {
  encode(message: PingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ip !== "") {
      writer.uint32(10).string(message.ip);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    if (message.size !== 0) {
      writer.uint32(24).int32(message.size);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ip = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.size = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PingRequest {
    return {
      ip: isSet(object.ip) ? globalThis.String(object.ip) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
    };
  },

  toJSON(message: PingRequest): unknown {
    const obj: any = {};
    if (message.ip !== "") {
      obj.ip = message.ip;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PingRequest>, I>>(base?: I): PingRequest {
    return PingRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PingRequest>, I>>(object: I): PingRequest {
    const message = createBasePingRequest();
    message.ip = object.ip ?? "";
    message.type = object.type ?? "";
    message.size = object.size ?? 0;
    return message;
  },
};

function createBaseStopRequest(): StopRequest {
  return { reason: "" };
}

export const StopRequest: MessageFns<StopRequest> = {
  encode(message: StopRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== "") {
      writer.uint32(10).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StopRequest {
    return { reason: isSet(object.reason) ? globalThis.String(object.reason) : "" };
  },

  toJSON(message: StopRequest): unknown {
    const obj: any = {};
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StopRequest>, I>>(base?: I): StopRequest {
    return StopRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StopRequest>, I>>(object: I): StopRequest {
    const message = createBaseStopRequest();
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseNetCheckReport(): NetCheckReport {
  return {
    now: undefined,
    udp: false,
    ipv6: false,
    ipv4: false,
    ipv6CanSend: false,
    ipv4CanSend: false,
    osHasIpv6: false,
    globalV4: "",
    globalV6: "",
    stunLatency: {},
    turnLatency: {},
    errors: [],
    cerfLatency: {},
    preferredCerfRegion: "",
    preferredCerfRegionId: 0,
  };
}

export const NetCheckReport: MessageFns<NetCheckReport> = {
  encode(message: NetCheckReport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.now !== undefined) {
      Timestamp.encode(toTimestamp(message.now), writer.uint32(10).fork()).join();
    }
    if (message.udp !== false) {
      writer.uint32(16).bool(message.udp);
    }
    if (message.ipv6 !== false) {
      writer.uint32(24).bool(message.ipv6);
    }
    if (message.ipv4 !== false) {
      writer.uint32(32).bool(message.ipv4);
    }
    if (message.ipv6CanSend !== false) {
      writer.uint32(40).bool(message.ipv6CanSend);
    }
    if (message.ipv4CanSend !== false) {
      writer.uint32(48).bool(message.ipv4CanSend);
    }
    if (message.osHasIpv6 !== false) {
      writer.uint32(56).bool(message.osHasIpv6);
    }
    if (message.globalV4 !== "") {
      writer.uint32(66).string(message.globalV4);
    }
    if (message.globalV6 !== "") {
      writer.uint32(74).string(message.globalV6);
    }
    globalThis.Object.entries(message.stunLatency).forEach(([key, value]: [string, number]) => {
      NetCheckReport_StunLatencyEntry.encode({ key: key as any, value }, writer.uint32(82).fork()).join();
    });
    globalThis.Object.entries(message.turnLatency).forEach(([key, value]: [string, number]) => {
      NetCheckReport_TurnLatencyEntry.encode({ key: key as any, value }, writer.uint32(90).fork()).join();
    });
    for (const v of message.errors) {
      writer.uint32(98).string(v!);
    }
    globalThis.Object.entries(message.cerfLatency).forEach(([key, value]: [string, number]) => {
      NetCheckReport_CerfLatencyEntry.encode({ key: key as any, value }, writer.uint32(106).fork()).join();
    });
    if (message.preferredCerfRegion !== "") {
      writer.uint32(114).string(message.preferredCerfRegion);
    }
    if (message.preferredCerfRegionId !== 0) {
      writer.uint32(120).uint32(message.preferredCerfRegionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetCheckReport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetCheckReport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.now = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.udp = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ipv6 = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.ipv4 = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.ipv6CanSend = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.ipv4CanSend = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.osHasIpv6 = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.globalV4 = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.globalV6 = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          const entry10 = NetCheckReport_StunLatencyEntry.decode(reader, reader.uint32());
          if (entry10.value !== undefined) {
            message.stunLatency[entry10.key] = entry10.value;
          }
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          const entry11 = NetCheckReport_TurnLatencyEntry.decode(reader, reader.uint32());
          if (entry11.value !== undefined) {
            message.turnLatency[entry11.key] = entry11.value;
          }
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.errors.push(reader.string());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          const entry13 = NetCheckReport_CerfLatencyEntry.decode(reader, reader.uint32());
          if (entry13.value !== undefined) {
            message.cerfLatency[entry13.key] = entry13.value;
          }
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.preferredCerfRegion = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.preferredCerfRegionId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetCheckReport {
    return {
      now: isSet(object.now) ? fromJsonTimestamp(object.now) : undefined,
      udp: isSet(object.udp) ? globalThis.Boolean(object.udp) : false,
      ipv6: isSet(object.ipv6) ? globalThis.Boolean(object.ipv6) : false,
      ipv4: isSet(object.ipv4) ? globalThis.Boolean(object.ipv4) : false,
      ipv6CanSend: isSet(object.ipv6CanSend)
        ? globalThis.Boolean(object.ipv6CanSend)
        : isSet(object.ipv6_can_send)
        ? globalThis.Boolean(object.ipv6_can_send)
        : false,
      ipv4CanSend: isSet(object.ipv4CanSend)
        ? globalThis.Boolean(object.ipv4CanSend)
        : isSet(object.ipv4_can_send)
        ? globalThis.Boolean(object.ipv4_can_send)
        : false,
      osHasIpv6: isSet(object.osHasIpv6)
        ? globalThis.Boolean(object.osHasIpv6)
        : isSet(object.os_has_ipv6)
        ? globalThis.Boolean(object.os_has_ipv6)
        : false,
      globalV4: isSet(object.globalV4)
        ? globalThis.String(object.globalV4)
        : isSet(object.global_v4)
        ? globalThis.String(object.global_v4)
        : "",
      globalV6: isSet(object.globalV6)
        ? globalThis.String(object.globalV6)
        : isSet(object.global_v6)
        ? globalThis.String(object.global_v6)
        : "",
      stunLatency: isObject(object.stunLatency)
        ? (globalThis.Object.entries(object.stunLatency) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : isObject(object.stun_latency)
        ? (globalThis.Object.entries(object.stun_latency) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
      turnLatency: isObject(object.turnLatency)
        ? (globalThis.Object.entries(object.turnLatency) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : isObject(object.turn_latency)
        ? (globalThis.Object.entries(object.turn_latency) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
      errors: globalThis.Array.isArray(object?.errors)
        ? object.errors.map((e: any) => globalThis.String(e))
        : [],
      cerfLatency: isObject(object.cerfLatency)
        ? (globalThis.Object.entries(object.cerfLatency) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : isObject(object.cerf_latency)
        ? (globalThis.Object.entries(object.cerf_latency) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
      preferredCerfRegion: isSet(object.preferredCerfRegion)
        ? globalThis.String(object.preferredCerfRegion)
        : isSet(object.preferred_cerf_region)
        ? globalThis.String(object.preferred_cerf_region)
        : "",
      preferredCerfRegionId: isSet(object.preferredCerfRegionId)
        ? globalThis.Number(object.preferredCerfRegionId)
        : isSet(object.preferred_cerf_region_id)
        ? globalThis.Number(object.preferred_cerf_region_id)
        : 0,
    };
  },

  toJSON(message: NetCheckReport): unknown {
    const obj: any = {};
    if (message.now !== undefined) {
      obj.now = message.now.toISOString();
    }
    if (message.udp !== false) {
      obj.udp = message.udp;
    }
    if (message.ipv6 !== false) {
      obj.ipv6 = message.ipv6;
    }
    if (message.ipv4 !== false) {
      obj.ipv4 = message.ipv4;
    }
    if (message.ipv6CanSend !== false) {
      obj.ipv6CanSend = message.ipv6CanSend;
    }
    if (message.ipv4CanSend !== false) {
      obj.ipv4CanSend = message.ipv4CanSend;
    }
    if (message.osHasIpv6 !== false) {
      obj.osHasIpv6 = message.osHasIpv6;
    }
    if (message.globalV4 !== "") {
      obj.globalV4 = message.globalV4;
    }
    if (message.globalV6 !== "") {
      obj.globalV6 = message.globalV6;
    }
    if (message.stunLatency) {
      const entries = globalThis.Object.entries(message.stunLatency) as [string, number][];
      if (entries.length > 0) {
        obj.stunLatency = {};
        entries.forEach(([k, v]) => {
          obj.stunLatency[k] = Math.round(v);
        });
      }
    }
    if (message.turnLatency) {
      const entries = globalThis.Object.entries(message.turnLatency) as [string, number][];
      if (entries.length > 0) {
        obj.turnLatency = {};
        entries.forEach(([k, v]) => {
          obj.turnLatency[k] = Math.round(v);
        });
      }
    }
    if (message.errors?.length) {
      obj.errors = message.errors;
    }
    if (message.cerfLatency) {
      const entries = globalThis.Object.entries(message.cerfLatency) as [string, number][];
      if (entries.length > 0) {
        obj.cerfLatency = {};
        entries.forEach(([k, v]) => {
          obj.cerfLatency[k] = Math.round(v);
        });
      }
    }
    if (message.preferredCerfRegion !== "") {
      obj.preferredCerfRegion = message.preferredCerfRegion;
    }
    if (message.preferredCerfRegionId !== 0) {
      obj.preferredCerfRegionId = Math.round(message.preferredCerfRegionId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NetCheckReport>, I>>(base?: I): NetCheckReport {
    return NetCheckReport.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NetCheckReport>, I>>(object: I): NetCheckReport {
    const message = createBaseNetCheckReport();
    message.now = object.now ?? undefined;
    message.udp = object.udp ?? false;
    message.ipv6 = object.ipv6 ?? false;
    message.ipv4 = object.ipv4 ?? false;
    message.ipv6CanSend = object.ipv6CanSend ?? false;
    message.ipv4CanSend = object.ipv4CanSend ?? false;
    message.osHasIpv6 = object.osHasIpv6 ?? false;
    message.globalV4 = object.globalV4 ?? "";
    message.globalV6 = object.globalV6 ?? "";
    message.stunLatency = (globalThis.Object.entries(object.stunLatency ?? {}) as [string, number][]).reduce(
      (acc: { [key: string]: number }, [key, value]: [string, number]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.turnLatency = (globalThis.Object.entries(object.turnLatency ?? {}) as [string, number][]).reduce(
      (acc: { [key: string]: number }, [key, value]: [string, number]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.errors = object.errors?.map((e) => e) || [];
    message.cerfLatency = (globalThis.Object.entries(object.cerfLatency ?? {}) as [string, number][]).reduce(
      (acc: { [key: string]: number }, [key, value]: [string, number]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.preferredCerfRegion = object.preferredCerfRegion ?? "";
    message.preferredCerfRegionId = object.preferredCerfRegionId ?? 0;
    return message;
  },
};

function createBaseNetCheckReport_StunLatencyEntry(): NetCheckReport_StunLatencyEntry {
  return { key: "", value: 0 };
}

export const NetCheckReport_StunLatencyEntry: MessageFns<NetCheckReport_StunLatencyEntry> = {
  encode(message: NetCheckReport_StunLatencyEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetCheckReport_StunLatencyEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetCheckReport_StunLatencyEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetCheckReport_StunLatencyEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: NetCheckReport_StunLatencyEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NetCheckReport_StunLatencyEntry>, I>>(base?: I): NetCheckReport_StunLatencyEntry {
    return NetCheckReport_StunLatencyEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NetCheckReport_StunLatencyEntry>, I>>(
    object: I,
  ): NetCheckReport_StunLatencyEntry {
    const message = createBaseNetCheckReport_StunLatencyEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseNetCheckReport_TurnLatencyEntry(): NetCheckReport_TurnLatencyEntry {
  return { key: "", value: 0 };
}

export const NetCheckReport_TurnLatencyEntry: MessageFns<NetCheckReport_TurnLatencyEntry> = {
  encode(message: NetCheckReport_TurnLatencyEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetCheckReport_TurnLatencyEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetCheckReport_TurnLatencyEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetCheckReport_TurnLatencyEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: NetCheckReport_TurnLatencyEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NetCheckReport_TurnLatencyEntry>, I>>(base?: I): NetCheckReport_TurnLatencyEntry {
    return NetCheckReport_TurnLatencyEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NetCheckReport_TurnLatencyEntry>, I>>(
    object: I,
  ): NetCheckReport_TurnLatencyEntry {
    const message = createBaseNetCheckReport_TurnLatencyEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseNetCheckReport_CerfLatencyEntry(): NetCheckReport_CerfLatencyEntry {
  return { key: "", value: 0 };
}

export const NetCheckReport_CerfLatencyEntry: MessageFns<NetCheckReport_CerfLatencyEntry> = {
  encode(message: NetCheckReport_CerfLatencyEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetCheckReport_CerfLatencyEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetCheckReport_CerfLatencyEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetCheckReport_CerfLatencyEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: NetCheckReport_CerfLatencyEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NetCheckReport_CerfLatencyEntry>, I>>(base?: I): NetCheckReport_CerfLatencyEntry {
    return NetCheckReport_CerfLatencyEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NetCheckReport_CerfLatencyEntry>, I>>(
    object: I,
  ): NetCheckReport_CerfLatencyEntry {
    const message = createBaseNetCheckReport_CerfLatencyEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

/**
 * HashiServiceはRunetale Clientのバックエンド専用のAPI's
 * hashilocalbackendのgrpc serviceとして実装されます
 */
export interface HashiService {
  Status(request: DeepPartial<Empty>, metadata?: grpc.Metadata): Promise<HashiStatus>;
  Ping(request: DeepPartial<PingRequest>, metadata?: grpc.Metadata): Promise<PingResult>;
  Login(request: DeepPartial<Empty>, metadata?: grpc.Metadata): Promise<HashiStatus>;
  Compose(request: DeepPartial<ComposeRequest>, metadata?: grpc.Metadata): Promise<HashiStatus>;
  Logout(request: DeepPartial<Empty>, metadata?: grpc.Metadata): Promise<HashiStatus>;
  Stop(request: DeepPartial<StopRequest>, metadata?: grpc.Metadata): Promise<HashiStatus>;
  Dial(request: DeepPartial<Empty>, metadata?: grpc.Metadata): Promise<HashiStatus>;
  NetCheck(request: DeepPartial<Empty>, metadata?: grpc.Metadata): Promise<NetCheckReport>;
}

export class HashiServiceClientImpl implements HashiService {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.Status = this.Status.bind(this);
    this.Ping = this.Ping.bind(this);
    this.Login = this.Login.bind(this);
    this.Compose = this.Compose.bind(this);
    this.Logout = this.Logout.bind(this);
    this.Stop = this.Stop.bind(this);
    this.Dial = this.Dial.bind(this);
    this.NetCheck = this.NetCheck.bind(this);
  }

  Status(request: DeepPartial<Empty>, metadata?: grpc.Metadata): Promise<HashiStatus> {
    return this.rpc.unary(HashiServiceStatusDesc, Empty.fromPartial(request), metadata);
  }

  Ping(request: DeepPartial<PingRequest>, metadata?: grpc.Metadata): Promise<PingResult> {
    return this.rpc.unary(HashiServicePingDesc, PingRequest.fromPartial(request), metadata);
  }

  Login(request: DeepPartial<Empty>, metadata?: grpc.Metadata): Promise<HashiStatus> {
    return this.rpc.unary(HashiServiceLoginDesc, Empty.fromPartial(request), metadata);
  }

  Compose(request: DeepPartial<ComposeRequest>, metadata?: grpc.Metadata): Promise<HashiStatus> {
    return this.rpc.unary(HashiServiceComposeDesc, ComposeRequest.fromPartial(request), metadata);
  }

  Logout(request: DeepPartial<Empty>, metadata?: grpc.Metadata): Promise<HashiStatus> {
    return this.rpc.unary(HashiServiceLogoutDesc, Empty.fromPartial(request), metadata);
  }

  Stop(request: DeepPartial<StopRequest>, metadata?: grpc.Metadata): Promise<HashiStatus> {
    return this.rpc.unary(HashiServiceStopDesc, StopRequest.fromPartial(request), metadata);
  }

  Dial(request: DeepPartial<Empty>, metadata?: grpc.Metadata): Promise<HashiStatus> {
    return this.rpc.unary(HashiServiceDialDesc, Empty.fromPartial(request), metadata);
  }

  NetCheck(request: DeepPartial<Empty>, metadata?: grpc.Metadata): Promise<NetCheckReport> {
    return this.rpc.unary(HashiServiceNetCheckDesc, Empty.fromPartial(request), metadata);
  }
}

export const HashiServiceDesc = { serviceName: "protos.HashiService" };

export const HashiServiceStatusDesc: UnaryMethodDefinitionish = {
  methodName: "Status",
  service: HashiServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return Empty.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = HashiStatus.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const HashiServicePingDesc: UnaryMethodDefinitionish = {
  methodName: "Ping",
  service: HashiServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return PingRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = PingResult.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const HashiServiceLoginDesc: UnaryMethodDefinitionish = {
  methodName: "Login",
  service: HashiServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return Empty.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = HashiStatus.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const HashiServiceComposeDesc: UnaryMethodDefinitionish = {
  methodName: "Compose",
  service: HashiServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return ComposeRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = HashiStatus.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const HashiServiceLogoutDesc: UnaryMethodDefinitionish = {
  methodName: "Logout",
  service: HashiServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return Empty.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = HashiStatus.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const HashiServiceStopDesc: UnaryMethodDefinitionish = {
  methodName: "Stop",
  service: HashiServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return StopRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = HashiStatus.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const HashiServiceDialDesc: UnaryMethodDefinitionish = {
  methodName: "Dial",
  service: HashiServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return Empty.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = HashiStatus.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const HashiServiceNetCheckDesc: UnaryMethodDefinitionish = {
  methodName: "NetCheck",
  service: HashiServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return Empty.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = NetCheckReport.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
